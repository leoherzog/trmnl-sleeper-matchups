{% render "main_content", 
    nfl_state: IDX_0,
    league_data: IDX_1,
    users_data: IDX_2,
    rosters_data: IDX_3,
    trmnl: trmnl,
    layout_size: "quadrant",
    status: status,
    week: week %}

{% comment %} Determine the effective week and status for display {% endcomment %}
{% assign effective_week = week | default: trmnl.plugin_settings.custom_fields_values.test_week | default: IDX_0.week | default: IDX_1.settings.leg %}
{% assign effective_status = status | default: trmnl.plugin_settings.custom_fields_values.test_status | default: IDX_1.status %}

{% render "title_bar",
    nfl_state: IDX_0,
    league_data: IDX_1,
    trmnl: trmnl,
    layout_size: "week",
    week: effective_week,
    status: effective_status %}
